
<div class = "container">
  <h1>Burgers!</h1>
  <div class="row">
    <div class="col-md-6">
      <h2>Burgers waiting to be eaten!</h2>

      <ul>
        {{#each burger}}
        {{#unless devoured}}
        <li>
          <button class="eat" data-id="{{id}}">
            Eat {{burger_name}}
          </button>
        </li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-6">
      <h2>Burgers that have been eaten!</h2>

      <ul>
        {{#each burger}}
        {{#if devoured}}
        <li>
          {{burger_name}} has been eaten
          {{/if}}
          {{/each}}
      </ul>
    </div>
  </div>




  <h2>Add a Burger</h2>
  <form class="create-form newBurger">
    <div class="form-group ">
      <label for="burger">Burger Name:</label>
      <input type="text" id="burger" name="name">
    </div>
    <button type="submit">Add Burger</button>
  </form>
</div>